#if !defined(EXAMPLE_HPP)
#define EXAMPLE_HPP

namespace temp
{
    class Example
    {
      private:
        /* data */
      public:
        Example();
        ~Example();
        int Test();
    };

} // namespace temp

#endif // EXAMPLE_HPP
